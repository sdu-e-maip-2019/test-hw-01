function approximated_value = squareroot(x, num_iterations)

approximated_value = 1;

for k = 1:num_iterations
    % Modify the following line, to make the function estimate the square
    % root of a number.
    approximated_value = approximated_value;
end

end